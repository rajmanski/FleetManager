root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/main ./cmd/server"
entrypoint = ["/app/tmp/main"]
include_ext = ["go"]
exclude_dir = ["tmp"]
stop_on_error = true
poll = true
poll_interval = 500

