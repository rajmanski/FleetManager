version: "2"
sql:
  - engine: "mysql"
    queries: "queries/"
    schema:
      - "migrations/"
      - "sqlc_schema/"
    gen:
      go:
        package: "sqlc"
        out: "internal/db/sqlc"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true

